language:
    - c
sudo:
    - required

before_install:
    - sudo apt-get update

install:
    - sudo apt install doxygen ninja-build

script:
    - mkdir build && cd build
    - cmake .. && ninja -j4 && ninja doc
